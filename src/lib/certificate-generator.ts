
'use client';

/**
 * @fileOverview Legacy jsPDF Generator (Deprecated).
 * Migration complete to unified HTML-to-Canvas template engine.
 * The system now uses the CertificateTemplate component for 100% design fidelity.
 */
export const generateCertificatePDF = () => {
  console.warn("Legacy generateCertificatePDF called. System has migrated to use html2canvas on CertificateTemplate.");
};
