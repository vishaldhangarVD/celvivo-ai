/**
 * @fileOverview Redundant component consolidated into CandidateHologram.tsx.
 * Removed during codebase deduplication and Three.js conflict resolution.
 */

export default function HologramFaceLoader() {
  return null;
}