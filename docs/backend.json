
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User account details and aggregate performance metrics.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "displayName": { "type": "string" },
        "email": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "founder"] },
        "credits": {
          "type": "object",
          "properties": {
            "aptitude": { "type": "number" },
            "coding": { "type": "number" },
            "interview": { "type": "number" }
          }
        },
        "subscription": {
          "type": "object",
          "properties": {
            "plan": { "type": "string", "enum": ["starter", "pro", null] },
            "status": { "type": "string" },
            "nextResetDate": { "type": "string", "format": "date-time" },
            "usage": {
              "type": "object",
              "properties": {
                "aptitude": { "type": "number" },
                "coding": { "type": "number" },
                "interview": { "type": "number" }
              }
            }
          }
        },
        "plan": { "type": "string", "description": "Legacy plan field for compatibility" },
        "jobReadinessScore": { "type": "number" },
        "resumeScore": { "type": "number" },
        "totalInterviews": { "type": "number" },
        "currentStreak": { "type": "number" },
        "bestStreak": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email"]
    }
  },
  "auth": {
    "providers": ["google", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/entities/UserProfile",
      "description": "Stores user profile, credits, and subscription status."
    }
  }
}
